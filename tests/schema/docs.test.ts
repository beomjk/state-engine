import { describe, it, expect } from 'vitest';
import { generateDocs, updateDocContent } from '../../src/schema/docs.js';
import { defineEntity, defineSchema } from '../../src/schema/define.js';

describe('generateDocs / updateDocContent', () => {
  it.todo('generates statuses table');
  it.todo('generates transitions table');
  it.todo('generates manual-transitions table');
  it.todo('updateDocContent replaces AUTO markers');
});
