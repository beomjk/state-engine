import { describe, it, expect } from 'vitest';
import { defineEntity, defineSchema, extractRules, extractManualTransitions } from '../../src/schema/define.js';

describe('defineEntity / defineSchema', () => {
  it.todo('defineEntity returns definition');
  it.todo('defineSchema returns schema');
  it.todo('extractRules converts transitions');
  it.todo('extractManualTransitions converts manual transitions');
});
